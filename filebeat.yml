filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/generated.log

output.logstash:
  hosts: ["logstash:5000"]

logging.level: info
logging.to_files: true
logging.files:
  path: /var/log/filebeat
